services:
  agent:
    build: .
    container_name: ambient-agent
    restart: no
    env_file: .env
    volumes:
      - ./data:/app/data          # token.json などの永続化
      - ./prompts:/app/prompts
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
