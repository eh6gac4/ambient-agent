# ambient-agent テスト設計書 (TEST_INFRA.md)

本ドキュメントは、`ambient-agent` プロジェクトにおけるテスト設計、モック戦略、および検証基準（Tiers 1-4）を定義します。

---

## 1. 機能インベントリとテスト手法

`ambient-agent` が提供するコア機能に対して、多様なテスト技法を適用します。

### A. スケジュール実行 (Cron) & 祝日スキップ
- **機能概要**: JST 05:20の事前準備、JST 05:30の日次ブリーフィング、および毎週のレポートなどをスケジュール実行し、土日・祝日は特定のジョブをスキップする。
- **テスト手法**:
  - **境界値分析 (BVA)**: 平日/週末の切り替わり境界時間（金曜夜〜土曜朝、月曜朝）、祝日の判定境界値（祝日の前日深夜〜当日）。
  - **デシジョンテーブル (Category-Partition)**: `(曜日: 平日/土日) × (祝日API判定: 祝日/平日) × (Cron式)` のマトリクスで、スキップされるべきジョブと実行されるべきジョブをすべて網羅。

### B. 位置情報トリガー (Geofence & Home Arrival / Office Leave)
- **機能概要**: ユーザーの位置情報（OwnTracks）を契機に、自宅到着（Home Arrival）および会社出発（Office Leave）を検知し、タスクをフィルタリングして Telegram に通知を送る。
- **テスト手法**:
  - **ペアワイズ法 (Pairwise)**: `(トリガー場所: 自宅/会社) × (タスクの状態: あり/なし/複数) × (位置情報の遷移: 圏外→圏内/圏内→圏外/同一)` を組み合わせた状態遷移テスト。
  - **実機再現 (Real-world / Scenario Testing)**: 実際に OwnTracks から送られてくる位置情報オブジェクト（緯度経度、タイムスタンプ）を模したシーケンスを作り、状態が正しく遷移するか（KVへの書き込み最適化を含めて）を検証。

### C. 位置情報ベースのタスク抽出・フィルタリング・重複排除
- **機能概要**: 自宅到着/会社出発時に、Notionタスクから該当するキーワード（家/自宅/home、オフィス/会社/職場/office）に紐づくタスクを強制抽出し、Gemini LLMによる抽出結果とマージして重複を排除する。
- **テスト手法**:
  - **カテゴリパーティション (Category-Partition)**:
    - 位置情報タグの表記揺れ（大文字小文字 `Home`/`home`、日本語表記 `自宅`/`家` 等）。
    - タスクの優先度（high, medium, low）の境界値とデフォルトフォールバック値。
  - **重複排除の同値分割**:
    - AI抽出タスクとルール抽出タスクが「完全に同一のタイトル」の場合。
    - 表記やスペースが異なるが意味的に重複している場合の処理（基本はタイトル完全一致）。
    - タスクが空の場合の Telegram への通知文言（「該当するタスクはありません。お疲れ様でした！」）。

---

## 2. 外部APIモック設計 (Mock Design)

E2Eテストおよび統合テストでは、Cloudflare Workerの実行環境で本物の外部APIを叩かずに、確定的なテストを実施するためにモック（Mocking）を適用します。

### A. Notion API
- **役割**: 未完了タスクの取得 (`getOpenTasks`)、タスク期限の更新、優先度変更。
- **モック設計**:
  - `cf-worker/test/fixtures/notion-tasks.json` のようなJSONモックデータを用意。
  - `vi.mock` を用いて、`getOpenTasks` が特定のタスクリストを返すようにスタブ化する。
  - タイトル、ロケーション（`location`）、期限（`due`）、優先度（`priority`）の様々なバリエーションを注入可能にする。

### B. Telegram API
- **役割**: ブリーフィングやタスク通知の送信。
- **モック設計**:
  - 送信されるテキスト内容を検証するため、`sendMessage` をスパイ（Spy）化。
  - Markdown V2 エスケープの処理が正しく行われているかを正規表現または文字列マッチでアサーション。
  - タスクが存在しない場合に「該当するタスクはありません」というメッセージが送信されることを確認する。

### C. Gemini API (Google AI SDK)
- **役割**: タスクの自動要約、状況に合わせた重要タスクのレコメンド抽出。
- **モック設計**:
  - `cf-worker/test/fixtures/gemini-responses.json` にレスポンスを定義。
  - テストケースごとに、Gemini の抽出結果を自由に制御（例: `[{ "title": "牛乳を買う", "priority": "medium" }]`）できるようにモックを差し替える。

### D. Holidays-JP API (日本の祝日 API)
- **役割**: `isHoliday` 関数内で `https://holidays-jp.github.io/api/v1/date.json` をフェッチし、日本の祝日判定を行う。
- **モック設計**:
  - `vi.stubGlobal("fetch", ...)` または `msw` (Mock Service Worker) に類する fetch モックを用い、特定のURLに対するGETリクエストに対して祝日のJSON（例: `{"2026-01-01": "元日"}`）を返却する。
  - システム時刻（`Date.now`）のモックと連動させ、判定対象日付の祝日設定を自在に変更する。

---

## 3. 最小閾値検証基準 (Minimum Threshold Verification - Tiers 1-4)

テストスイートの品質保証レベルを4つのティアに分類し、マージブロックなどのCI/CD条件として運用します。

| Tier | カテゴリ | 実行頻度 / トリガー | 成功・カバレッジ閾値 | 検証対象と内容 |
| :--- | :--- | :--- | :--- | :--- |
| **Tier 1** | **Unit Tests (単体テスト)** | コミットごと / ローカル保存時 | - カバレッジ: 80%以上<br>- パス率: 100% | ユーティリティ、計算ロジック（JST変換、優先度アイコン付与、祝日API判定関数、タスク整形処理など）の単体検証。 |
| **Tier 2** | **Integration Tests (結合テスト)** | PR作成時 / CIビルド時 | - パス率: 100% | 複数コンポーネント間の連携（KV状態管理、D1データベース読み書き、Cronジョブからの各ハンドラー起動経路のディスパッチ検証）。 |
| **Tier 3** | **E2E Workflow Tests (E2Eテスト)** | PRマージ前 / リリース前 | - パス率: 100% | 位置情報受信からTelegram通知までの実ワークフローのシミュレーション（祝日時の挙動、タスク抽出ロジック、重複排除、空通知ハンドリング）。 |
| **Tier 4** | **Real-world / Sandbox Tests (本番相当検証)** | リリース後定期実行 / 手動実行 | - エラー率: 0.1%未満 | 実際のSandbox環境または本番と同等のデータソースを用いて、外部APIのレートリミット超過やタイムアウトエラーが正しく処理されるかの耐障害性テスト。 |
